<section class="regular_slider slider">
  <% list.each do |item| %>
    <div>
      <%#= image_tag("0.jpg", width: '120', height: '120') %>
      <%= link_to(image_tag(chi item), item) %>
      <div class="slider-item-title"><%= item.title %></div>
    </div>
  <% end %>
</section>