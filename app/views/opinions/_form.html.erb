<%= form_for(opinion) do |f| %>
  <% if opinion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opinion.errors.count, "error") %> prohibited this opinion from being saved:</h2>

      <ul>
      <% opinion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Ваш отзыв:" %>
    <%= f.text_area :optext, class: "large-text-area" %>
  </div>

  <div class="field">
    <%= f.hidden_field :user_id, :value => user_id %>
  </div>  

  <div class="actions">
    <%= f.submit "Оставить отзыв" %>
  </div>
<% end %>
