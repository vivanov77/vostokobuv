<%= form_for([:admin, news]) do |f| %>
  <% if news.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(news.errors.count, "error") %> prohibited this news from being saved:</h2>

      <ul>
      <% news.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Наименование" %>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label "Содержимое" %>
    <%= f.text_area :content %>
  </div>

  <div class="field">
    <%= f.label "Отправлено" %>
    <%= f.check_box :sent %>
  </div>

  <div class="actions">
    <%= f.submit submit_russian_text "Новость" %>
  </div>
<% end %>
