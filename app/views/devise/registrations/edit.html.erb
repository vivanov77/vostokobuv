<h1>Сменить пароль</h1>

<%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put, class:"form-horizontal" }) do |f| %>
  <%= devise_error_messages! %>

  <div class="form-group">
    <div class="field">
      <%= f.label "Электронная почта", class: "col-sm-3 control-label nowrap" %>
      <div class="col-sm-6">
      <%= f.email_field :email, class: "form-control", placeholder: "Электронная почта" %>
      </div>
    </div>
  </div>

  <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
    <div>В данный момент ожидаю подтверждение для: <%= resource.unconfirmed_email %></div>
  <% end %>

  <div class="form-group">

    <div class="col-sm-offset-3 padding-left-15">
      <i>(оставьте пустым, если Вы не хотите менять его)</i>
    </div>  

    <div class="field">
      <%= f.label "Пароль", class: "col-sm-3 control-label nowrap" %>
      <div class="col-sm-6">   
      <%= f.password_field :password, autocomplete: "off", class: "form-control", placeholder: "Пароль" %>
      </div>
      <div class="col-sm-offset-3 col-sm-6">
        <% if @minimum_password_length %>
          <em>Минимальное количество знаков: <%= @minimum_password_length %></em>
        <% end %> 
      </div>      
    </div>
  </div>


  <div class="form-group">
    <div class="field">
      <%= f.label "Подтверждение пароля", class: "col-sm-3 control-label nowrap" %>
      <div class="col-sm-6">
      <%= f.password_field :password_confirmation, autocomplete: "off", class: "form-control", placeholder: "Подтверждение пароля" %>
      </div>
    </div>
  </div>

<!--   <div class="field">
    <%#= f.label :current_password %> <i>(we need your current password to confirm your changes)</i><br />
    <%#= f.password_field :current_password, autocomplete: "off" %>
  </div> -->

  <div class="form-group">
    <div class="col-sm-offset-3 col-sm-6 actions">
      <%= f.submit "Обновить", class:"green-button button-height" %>
    </div>
  </div>

<% end %>

<h3>Удалить мою учётную запись</h3>

<p>Недовольны?</p>

<div class="col-sm-5">
 <%= button_to "Удалить мою учётную запись", registration_path(resource_name), data: { confirm: "Вы уверены?" }, method: :delete, class:"green-button button-height width-100-percent" %>
</div>

<%#= link_to "Back", :back %>
